<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{{ title + " | " if not is_home }}Fynn Becker</title>
    <meta name="description" content="{{ description }}">

    <link rel="icon" href="/favicon.svg">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/mask-icon.svg" color="#d72828">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <meta name="theme-color" content="#d72828">

    <meta name="twitter:card" content="{{ twitter.card or "summary" }}">
    <meta name="twitter:site" content="@mvsde">
    <meta name="twitter:title" content="{{ (twitter.title or title) | truncate(70) }}">
    <meta name="twitter:description" content="{{ (twitter.description or description) | truncate(200) }}">
    <meta name="twitter:image" content="{{ "https://fynn.be" if env === "production" }}{{ twitter.image.src }}">
    <meta name="twitter:image:alt" content="{{ twitter.image.alt | truncate(420) }}">

    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>

    {% include "header.njk" %}

    <main class="content">

      <h1 class="content__title">
        {% block title %}
          {{ title }}
        {% endblock %}
      </h1>

      <div class="content__wrapper {% if hero %}content__wrapper--hero{% endif %}">
        {% if hero %}
          {% include "hero.njk" %}
        {% endif %}

        <div class="content__inner container">
          {% block content %}
            {{ content | safe }}
          {% endblock %}
        </div>

      </div>

    </main>

    {% include "footer.njk" %}

  </body>
</html>
